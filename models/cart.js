const db = require('../util/database');

module.exports = class Cart {
    static addProduct(id, productTitle, productPrice) {
        
    };

    static deleteProduct(id, productPrice) {

    };

    static getCart(cb) {

    };
};
